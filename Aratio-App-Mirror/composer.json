{
    "name": "aratio/crm",
    "description": "Aratio CRM - Sistema de Gestión Social",
    "type": "project",
    "require": {
        "php": ">=8.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.0"
    },
    "autoload": {
        "classmap": [
            "includes/",
            "config/",
            "api/"
        ]
    },
    "scripts": {
        "test": "phpunit",
        "lint": "php -l config/*.php includes/*.php api/*.php pages/*.php"
    }
}
