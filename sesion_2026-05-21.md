# ARATIO — Sesión de Trabajo 2026-05-21

## Resumen ejecutivo

Implementación del plan de sincronización de Instagram (plan de IA externa):
1. Creación de `api/instagram_sync.php` (endpoint start/status)
2. Mejora de `pages/actividad_instagram.php` (botón sync + modal Alpine.js con polling)
3. Optimización masiva del scraper `instagram_scraper.py`:
   - Nuevo método `scrape_api_paginated()` — extracción vía API GraphQL de Instagram con paginación real (sin Selenium, obtiene todos los posts)
   - Scroll mejorado como fallback (60 scrolls multi-step con detección de carga)
   - Default `max_posts` subido de 100 → 300
4. Despliegue a producción via `deploy_quick.py`

## Cambios posteriores

- Implementado modo **mes a mes** (`--monthly`):
  1. Fase 1: Selenium abre Instagram → extrae cookies reales + user_id + query_hash → cierra Selenium → pagina toda la API GraphQL con requests (rápido, sin límite de scroll)
  2. Fase 2 (fallback si API da <50 posts): Selenium mes a mes desde enero 2024, cada mes un pass independiente
- `max_posts` subido a 5000
- API endpoint actualizado con `--monthly`

## Pendiente

- Probar **Sincronizar Actividad** en local después del reinicio
- Verificar que la API GraphQL traiga todos los posts (debería obtenerlos todos en segundos)
- Si la API falla, cae automáticamente a Selenium mes a mes (lento pero completo)
